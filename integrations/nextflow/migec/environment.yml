---
# yaml-language-server: $schema=https://raw.githubusercontent.com/nf-core/modules/master/modules/environment-schema.json
channels:
  - conda-forge
  - bioconda
dependencies:
  - pip
  - python>=3.10
  - pip:
      # Pre-release until 2.0.0 final; conda's pip honours the explicit version.
      - migec==2.0.0a1
